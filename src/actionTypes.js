
const API = 'apiService/REQ_'

const ActionTypes = {
    req: {
        TeamSugiInfo: `${API}TEAMSUGIINFO`,
        DeleteSufi:    `${API}DELETESUGI`,
        SaveSugi : `${API}SAVESUGI`,
        UpdateSugi : `${API}UPDATESUGI`,
    }
}

export default ActionTypes